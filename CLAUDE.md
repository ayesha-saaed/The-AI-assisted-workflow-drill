Forms must strictly use React controlled components (useState) for all inputs.

All form fields must include inline validation error messages styled with Tailwind (e.g., text-red-500).

Never generate uncontrolled inputs or omit htmlFor accessibility tags on form labels.